schema = 1
project = "forebay/managed"
version = "1.0.0"

[modules]

[plugins]
stub-toolchain-config = "./plugins/stub-toolchain-config.lua"

[toolchains.stub]
version = "1"
target = "app"
flag = "on"

  [toolchains.stub.dependencies."forebay/basekit"]
  version = "^5.0.0"
